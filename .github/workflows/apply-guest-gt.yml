name: Apply calculator patch
'on': [push]
permissions:
  contents: write
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Patch index
        run: python .github/patch_guest_gt.py
      - name: Fix calendar
        run: python .github/calendar_fix.py
      - name: Apply stay enhancements
        run: python .github/stay_enhancements.py
      - name: Commit patched index
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add index.html
          git diff --cached --quiet || (git commit -m 'Add Nights stepper and service date defaults' && git push)
